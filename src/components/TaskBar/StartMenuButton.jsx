const StartMenuButton = (props) => {
  return (
    <div {...props} className="logo-wrapper" title="Start">
      <div className="logo"></div>
    </div>
  );
};

export default StartMenuButton;
