const SearchBar = (props) => {
  return (
    <div className="search">
      <span className="InputAddOn-item"></span>
      <input className="InputAddOn-field" placeholder="Type here to search" />
    </div>
  );
};

export default SearchBar;
